## 📌 專案當前技術架構簡述

- **前端：** ReactJS 部署於 GitHub Pages 。
    
- **後端：** Python FastAPI 部署於 Vercel Serverless 。
    
- **資料庫：** Supabase REST API (採用 `supabase-py` 官方 HTTP SDK) 。
    
- **密碼雜湊：** 獨立的 `bcrypt` 套件 。
    

## 💾 第一部分：資料庫工作區 (Database Workspace) 修改需求

請在 Supabase SQL Editor 中針對 Phase 1 已存在的資料表進行結構升級（改用 `ALTER TABLE` 或微調定義）：

### 1. 建立列舉型態 (Enums)

為了增強資料庫的防禦性，建立學制代碼與學籍狀態的列舉：

SQL

```
-- 根據世新學號首碼定義的學制別
CREATE TYPE degree_type AS ENUM ('A', 'C', 'M', 'S', 'D');

-- 使用者目前的學籍狀態
CREATE TYPE enrollment_status AS ENUM ('Active', 'Suspended', 'Graduated');
```

### 2. 升級 `card_profiles` 資料表

修改現有的 `card_profiles` 表，將單一身份字串升級為支援「身份重疊」的**文字陣列（Array）**，並引入**動態展延有效期限**機制：

SQL

```
-- 1. 刪除舊的單一身份欄位（若原本有設定 identity_type）
ALTER TABLE card_profiles DROP COLUMN IF EXISTS identity_type;

-- 2. 新增與修改升級欄位
ALTER TABLE card_profiles 
  -- 升級為陣列，預設值為 Student，可容納多重組織身份如 ['Alumni', 'Faculty']
  ADD COLUMN IF NOT EXISTS identities TEXT[] DEFAULT '{Student}',
  -- 儲存解析後的學制首碼 (A/C/M/S/D)
  ADD COLUMN IF NOT EXISTS degree_code degree_type NOT NULL DEFAULT 'A',
  -- 當前學籍現況，預設為 Active (在學)
  ADD COLUMN IF NOT EXISTS current_status enrollment_status NOT NULL DEFAULT 'Active',
  -- 所屬學級（例如 111 級，由學號自動解析）
  ADD COLUMN IF NOT EXISTS class_generation INT NOT NULL DEFAULT 111,
  -- 預計畢業學年度（行政自動化比對基準）
  ADD COLUMN IF NOT EXISTS expected_graduation_year INT NOT NULL DEFAULT 115,
  -- 方案 A 核心：數位系卡本學期的有效期限
  ADD COLUMN IF NOT EXISTS valid_until TIMESTAMP WITH TIME ZONE,
  -- 記錄最後一次提交在學認證的時間
  ADD COLUMN IF NOT EXISTS last_verified_at TIMESTAMP WITH TIME ZONE;
```

## ⚙️ 第二部分：後端工作區 (Backend Workspace) 修改需求

Agent 請針對 `backend/` 目錄底下的核心邏輯進行修改與擴充，**嚴格遵循：身分（Identities）不等於權限（System Role）之原則。**

### 1. 學號自動解析模組：`backend/core/utils.py`

當學生首次建立系卡（`POST /api/card/setup`）並輸入學號（如 `A111070036`）時，系統需自動解析學制、所屬學級與最長修業年限：

Python

```
# backend/core/utils.py
def parse_student_id_details(student_id: str) -> tuple:
    """
    解析世新大學學號邏輯
    例如: A111070036 -> degree_code='A', class_generation=111, expected_graduation_year=115
    """
    if not student_id or len(student_id) < 4:
        raise ValueError("學號格式不正確")
        
    degree_code = student_id[0].upper() # 取首碼 A, C, M, S, D
    
    try:
        class_generation = int(student_id[1:4]) # 取 111 入學年度
    except ValueError:
        raise ValueError("學號年度解析錯誤")
        
    # 世新學制標準修業年限對照表
    duration_mapping = {
        'A': 4,  -- 日間部四年制學士班 (一般大學生)
        'C': 4,  -- 進修學士班 (夜間部)
        'M': 2,  -- 碩士班 (研究所)
        'S': 2,  -- 碩士在職專班
        'D': 4   -- 博士班
    }
    
    duration = duration_mapping.get(degree_code, 4)
    expected_graduation_year = class_generation + duration
    
    return degree_code, class_generation, expected_graduation_year
```

### 2. 修正與升級安全驗證攔截器：`backend/api/deps.py`

請 Agent 修正 `deps.py`。**教職員不再自動擁有後台權限**。系統僅依據 `users.system_role` 區分權限（Admin / Manager / Contributor），並針對在校學生（Student）實施「方案 A 效期攔截」：

Python

```
# backend/api/deps.py
from fastapi import HTTPException, Security, status
from datetime import datetime, timezone

def verify_card_status_and_validity(profile: dict, user_system_role: str):
    """
    核心權限與效期攔截器
    """
    identities = profile.get("identities", [])
    current_status = profile.get("current_status", "Active")
    valid_until_str = profile.get("valid_until")
    
    # 防禦 1：休學/停用狀態攔截（立刻暫停系卡與特約商店功能）
    if current_status == "Suspended":
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="您的學籍目前處於暫停狀態，數位系卡與特約商店功能已暫時凍結。如有疑問請洽系辦。"
        )
        
    # 防禦 2：針對具有 Student 身分者，執行方案 A 展延效期檢查
    if "Student" in identities and current_status == "Active":
        if valid_until_str:
            # 轉換 Supabase 的 ISO 時間字串
            valid_until = datetime.fromisoformat(valid_until_str.replace("Z", "+00:00"))
            if datetime.now(timezone.utc) > valid_until:
                raise HTTPException(
                    status_code=status.HTTP_403_FORBIDDEN,
                    detail="您的數位系卡本學期已過期，請上傳新學期『在學證明』進行在校認證展延。"
                )
        else:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="尚未進行當學期在校認證。"
            )
            
    return True

# 系統權限群組檢查常規函數 (用於各 API 節點保護)
def require_manager_or_admin(system_role: str):
    # 活動管理員(Manager)與系統管理員(Admin)皆可新增最新消息與管理特約商家
    if system_role not in ["Manager", "Admin"]:
        raise HTTPException(status_code=403, detail="權限不足，限活動管理員或系統管理員操作")

def require_admin_only(system_role: str):
    # 只有系統管理員(Admin)可以編輯用戶權限
    if system_role != "Admin":
        raise HTTPException(status_code=403, detail="權限不足，僅限系統管理員操作")
```

### 3. 新增「在學證明上傳與展延審核」API：`backend/api/card.py`

配合方案 A 與全新的權限分組規範，實作在校認證端點：

Python

```
# backend/api/card.py
from fastapi import APIRouter, Depends, UploadFile, File
from datetime import datetime, timezone

router = APIRouter()

@router.post("/upload-enrollment-proof")
def upload_enrollment_proof(
    file: UploadFile = File(...), 
    current_user: dict = Depends(get_current_user)
):
    """
    【一般用戶 Contributor 可執行】
    學生端：上傳世新大學在學證明 PDF 或截圖
    """
    return {"message": "在學證明已成功上傳，等待管理員審核。"}

@router.post("/verify-and-extend/{target_user_id}")
def verify_and_extend_card(
    target_user_id: str, 
    semester: int,  # 1 代表上學期, 2 代表下學期
    current_user: dict = Depends(get_current_user) 
):
    """
    【活動管理員 Manager / 系統管理員 Admin 可執行】
    審核在學證明並執行一鍵展延
    """
    # 驗證操作者權限
    require_manager_or_admin(current_user.get("system_role"))
    
    now = datetime.now(timezone.utc)
    
    # 計算展延過期時間（依據世新學期核發慣例切分）
    if semester == 1:
        # 上學期，有效期限核發到隔年 2 月 15 日
        next_year = now.year + 1 if now.month >= 8 else now.year
        valid_date = datetime(next_year, 2, 15, tzinfo=timezone.utc)
    else:
        # 下學期，有效期限核發到當年 9 月 15 日
        valid_date = datetime(now.year, 9, 15, tzinfo=timezone.utc)
        
    # [呼叫 Supabase REST API 更新該使用者的有效期限與狀態]
    # Update card_profiles set valid_until = valid_date, current_status = 'Active' where user_id = target_user_id
    
    return {"message": f"審核成功！該學生數位系卡已展延至 {valid_date.strftime('%Y-%m-%d')}"}
```

### 4. 編輯用戶權限專屬 API：`backend/api/admin.py` (新創)

落實系統管理員（Admin）的核心職責，允許其直接指派、編輯用戶的系統權限群組（System Role）：

Python

```
# backend/api/admin.py
from fastapi import APIRouter, Depends

router = APIRouter()

@router.put("/admin/update-user-role/{target_user_id}")
def update_user_system_role(
    target_user_id: str,
    new_role: str, -- 'Admin', 'Manager', 或 'Contributor'
    current_user: dict = Depends(get_current_user)
):
    """
    【僅限系統管理員 Admin 可執行】
    變更、指派使用者的後台系統權限組
    """
    # 嚴格防禦：僅限 Admin
    require_admin_only(current_user.get("system_role"))
    
    if new_role not in ["Admin", "Manager", "Contributor"]:
        raise HTTPException(status_code=400, detail="無效的權限組名稱")
        
    # [呼召 Supabase REST API 更新該用戶的 system_role]
    # Update users set system_role = new_role where id = target_user_id
    
    return {"message": f"用戶權限已成功更新為 {new_role}"}
```

### 5. 修正自動畢業排程 (FastAPI Cron Job)

修改每年 7 月 1 日自動執行的畢業偵測腳本：

- **邏輯：** 只有當 `expected_graduation_year <= 當前學年度` 且 `current_status = 'Active'`（非休學）時，系統才自動在 `identities` 陣列中追加 `'Alumni'` 標籤 。其系統權限（`system_role`）維持不變，不自動覆蓋或提升。
    

## 🖥️ 第三部分：前端工作區 (Frontend Workspace) 修改提示

- **特約商店防禦：** 當後端攔截並回傳 403 異常（系卡過期或暫停）時，前端 React 數位系卡面應動態加上 **「已失效 / Expired」** 浮水印 ，並隱藏/停用簽到與特約優惠 QR Code 。
    
- **多重身分切換：** 若使用者的 `identities` 陣列中同時包含多個標籤（例如 `["Alumni", "Faculty"]`） ，前端卡面應提供一鍵翻轉切換機制，允許其在不同場合出示正確的組織身分卡面視覺 。
    

**請 Antigravity IDE Agent 依據此修正版需求說明書，開始對資料庫及後端程式碼進行修改與優化。**